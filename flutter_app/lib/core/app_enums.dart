enum UserRole {
  admin,
  customer,
}

enum CouponStatus {
  active,
  expired,
  disabled,
}

enum PlanVisibility {
  public,
  hidden,
}

enum PaymentStatus {
  pending,
  success,
  failed,
  refunded,
}

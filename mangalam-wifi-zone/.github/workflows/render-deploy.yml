name: Deploy to Render

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run Render Deploy
        run: |
          chmod +x ./automation/render_deploy.sh
          ./automation/render_deploy.sh

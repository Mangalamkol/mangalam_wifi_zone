{
  "name": "mangalam-wifi-zone-monorepo",
  "private": true,
  "scripts": {
    "install:all": "cd admin && npm install && cd ../server && npm install",
    "build:admin": "cd admin && npm run build && rm -rf ../server/public/admin && mkdir -p ../server/public/admin && cp -R dist/* ../server/public/admin/",
    "build:client": "cd client && flutter build web --release",
    "build": "npm run build:admin && npm run build:client",
    "start": "cd server && node server.js",
    "dev": "concurrently \"cd server && npm run dev\" \"cd admin && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "cors": "^2.8.5",
    "express-rate-limit": "^8.2.1",
    "firebase-admin": "^13.6.0",
    "joi": "^18.0.2",
    "node-cron": "^4.2.1",
    "randomstring": "^1.3.1",
    "winston": "^3.19.0"
  }
}
